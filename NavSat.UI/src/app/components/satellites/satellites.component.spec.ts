import { TestBed } from '@angular/core/testing';
import { SatelliteComponent } from './satellites.component';

describe('SatelliteComponent', () => {
  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [
        SatelliteComponent
      ],
    }).compileComponents();
  });
});
